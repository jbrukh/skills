# Skills Repo

## Post-commit install

After committing and pushing changes to skills, install them globally:

```
npx skills add jbrukh/skills --global --all --yes
```

Changes must be pushed to GitHub before running this, since the CLI clones the remote repo.
